services:
  backend:
    build:
      context: ./services/backend
    env_file:
      - ./.env
    ports:
      - "8000:8000"
    restart: unless-stopped

  bot:
    build:
      context: ./services/bot
    env_file:
      - ./.env
    depends_on:
      - backend
    restart: unless-stopped
